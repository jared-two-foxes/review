[package]
name = "implement-app"
version = "0.1.0"
edition = "2024"

[dependencies]
agent-kernel = { version = "0.1.0", path = "../agent-kernel" }
agent-protocol = { version = "0.1.0", path = "../agent-protocol" }
code-agent-runtime = { version = "0.1.0", path = "../code-agent-runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
