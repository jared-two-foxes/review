[package]
name = "implement-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
cli-common = { path = "../cli-common" }
implement-app = { path = "../implement-app" }
serde = { version = "1", features = ["derive"] }
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
ctrlc = "3"

[dev-dependencies]
serde_json = "1"
