[package]
name = "agent-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
agent-protocol = { path = "../agent-protocol" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
jsonschema = "0.29"
tracing = "0.1"
